# The data/scoring layer needs only PyYAML + numpy + scipy — `make test` and
# `make local` (scripts/run_local.py) run with just those, no GPU and no torch.
# Everything below is needed only for inference and training.
#
# CPU-only torch:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# Core ML stack
torch>=2.1.0
transformers>=4.44.0
datasets>=2.20.0
peft>=0.12.0
trl>=0.10.0,<0.21.0      # SFTConfig/SFTTrainer API churns; see coref/modeling/collator.py
bitsandbytes>=0.43.0     # 4-bit quantization (GPU only)
accelerate>=0.33.0

# Optional but strongly recommended: faster training + quantization-aware LoRA
# Install with: pip install "unsloth[colab-new]" or pip install unsloth
# unsloth

# Flash Attention 2 (optional, speeds up attention on Ampere+ GPUs)
# pip install flash-attn --no-build-isolation

# CEAFe optimal assignment (falls back to greedy if not installed)
scipy>=1.11.0

# Config parsing
PyYAML>=6.0

# Utilities
numpy>=1.24.0
tqdm>=4.66.0
